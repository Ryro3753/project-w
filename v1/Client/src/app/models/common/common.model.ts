export interface pageEmit{
    firstIndex: number;
    lastIndex: number;
}

export interface OnlyUserId{
    UserId: string;
}

export interface ShareRequest{
    ObjectId: number;
    Username: string;
}